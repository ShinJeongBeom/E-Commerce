# Architecture Guide

## 계층과 의존성

- Controller: 요청 변환, Bean Validation, 인증 주체 전달과 HTTP 응답 변환
- Application/Service: 유스케이스, 트랜잭션, 권한·소유권·상태 전이 검증
- Domain: 핵심 규칙과 불변식
- Repository/Infrastructure: 현재 JPA를 사용한다. QueryDSL, Redis 고급 활용, S3 연동은 필요 시 별도 작업으로 추가한다.
- DTO와 Entity를 분리하고 Controller가 Repository를 직접 호출하지 않는다.
- 기존 패키지 구조가 있으면 이를 우선하고 새 추상화는 복잡성을 실제로 줄일 때만 추가한다.
- 현재 패키지는 `auth`, `member`, `product`, `cart`, `order` 도메인 중심 구조다.
- `app/usecase/command/result` 구조는 향후 리팩터링 목표이며 현재 기능 연동 단계에서 무리하게 도입하지 않는다.

## 트랜잭션과 정합성

- 쓰기 트랜잭션은 Service 유스케이스 경계에 둔다.
- 조회는 가능한 경우 `readOnly = true`를 사용한다.
- 외부 API와 S3 호출을 긴 DB 트랜잭션 안에서 수행하지 않는다.
- DB 커밋 후 캐시 무효화가 필요하면 transaction synchronization 또는 이벤트 패턴을 사용한다.
- 결제처럼 원자적 DB 트랜잭션으로 묶을 수 없는 흐름은 멱등 키, 상태 머신과 보상 처리를 사용한다.
- 이벤트 발행 유실이 치명적이면 Outbox 패턴을 검토한다. 요구 범위 없이 먼저 도입하지 않는다.

## 오류 처리

- 예상 가능한 업무 오류는 도메인별 오류 코드로 표현한다.
- 전역 예외 처리기는 일관된 오류 응답을 만들고 내부 예외·SQL·스택 트레이스를 노출하지 않는다.
- `400` 검증 오류, `401` 미인증, `403` 권한 부족, `404` 미존재, `409` 상태 충돌을 구분한다.
- 외부 서비스 실패는 재시도 가능 여부를 구분하고 타임아웃을 설정한다.

## 시간·식별자·금액

- 서버 기본 타임존에 의존하지 않고 프로젝트의 UTC/Asia-Seoul 정책을 명시한다.
- 테스트 가능한 `Clock`을 주입한다.
- 외부 노출 식별자와 내부 PK 정책은 기존 구조를 따른다.
- 금액 계산에 부동소수점을 사용하지 않는다. 통화 최소 단위 `long` 또는 금액 Value Object를 사용한다.

## 관측성

- 요청 추적이 가능하도록 request/correlation ID를 전달한다.
- 로그에는 도메인 작업, 익명화된 식별자, 결과와 오류 코드를 구조적으로 남긴다.
- 토큰, 비밀번호, 개인정보, 결제 원문과 전체 요청 본문은 로그에 남기지 않는다.
- 외부 API 지연·실패, 캐시 hit/miss, 주문·결제 상태 전이에는 필요한 metric을 검토한다.
- health endpoint는 애플리케이션 생존과 MySQL·Redis 준비 상태를 구분한다.

## 성능

- N+1, 무제한 목록 조회와 불필요한 eager loading을 피한다.
- 쿼리 최적화는 실행 계획과 측정 결과를 근거로 한다.
- 캐시는 측정된 읽기 병목과 명확한 무효화 전략이 있을 때 사용한다.
- 동시성 전략은 낙관적 락, 비관적 락, 조건부 갱신 중 실제 충돌 특성에 맞춰 선택한다.
