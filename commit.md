# 커밋 작성 규칙

## 목표

작업을 기능 단위로 나눠 커밋하고, 커밋 메시지에서 변경 성격과 이슈 번호를 바로 확인할 수 있게 한다.

## 메시지 형식

```text
<prefix> : #<issue-number> <수정 내용>
```

예시:

```text
feat : #1 유저 도메인 생성
feat : #1 엔드포인트 권한 설정
chore : #1 Redis 및 JWT 설정 추가
refactor : #1 인증 구조 분리
fix : #1 토큰 재발급 검증 로직 수정
hotfix : #1 인증 필터 긴급 수정
```

## Prefix 기준

- `feat`: 신규 기능 또는 도메인 개발
- `fix`: 기존 동작의 오류 수정
- `refactor`: 외부 동작 변화 없는 구조 개선
- `hotfix`: 운영 장애 또는 즉시 반영이 필요한 긴급 오류 수정
- `chore`: 동작 변화 없는 설정, 문서, 빌드와 도구 변경

테스트만 독립적으로 추가하는 경우 저장소 정책이 허용하면 `test`를 사용할 수 있다. 저장소에서 허용하는
prefix가 따로 있으면 기존 커밋과 팀 규칙을 우선한다.

## 작성 규칙

- prefix 뒤에는 `공백 + 콜론 + 공백`을 둔다.
- 이슈 번호는 `#1`처럼 작성한다.
- 이슈 번호가 제공되지 않았으면 임의 번호를 만들지 않는다. 사용자에게 확인하거나 커밋을 보류한다.
- 메시지는 한국어로 간결하게 작성한다.
- 제목 한 줄만 읽어도 변경 결과가 드러나게 한다.
- 한 커밋에는 하나의 큰 의도만 담는다.
- 여러 파일이 바뀌었어도 같은 의도라면 함께 묶을 수 있다.
- 설정 변경과 기능 구현은 가능하면 분리한다.
- 테스트가 기능과 직접 연결되어 있으면 같은 커밋에 포함할 수 있다.
- 커밋마다 빌드 가능한 상태를 유지하고 이전 커밋의 테스트를 의도적으로 깨지 않는다.

## 큰 diff 분리 규칙

diff가 크면 파일 단위보다 사용자의 요구사항과 구현 의도를 기준으로 나눈다.

커밋 분리 순서:

1. 사용자가 요청한 요구사항 문장을 확인한다.
2. 요구사항을 기능, 설정, 구조 개선과 버그 수정 단위로 나눈다.
3. 각 단위에 맞는 prefix를 고른다.
4. 해당 단위에 필요한 파일만 stage한다.
5. 같은 파일에 여러 의도가 섞이면 가능한 경우 hunk 단위로 stage한다.
6. staged diff가 다른 커밋의 변경에 의존하는지 확인한다.
7. 각 커밋을 메시지 한 줄로 단독 설명할 수 있는지 확인한다.

분리 기준:

- 도메인 모델 추가: `feat`
- API 추가: `feat`
- 인증·권한 필터 추가: `feat`
- `build.gradle`, `application.yml`, SQL 경로와 문서 설정: `chore`
- 이름 변경, 패키지 이동과 구조 정리만 수행: `refactor`
- 기존 동작의 버그 수정: `fix`
- 운영 장애 또는 긴급 수정: `hotfix`

큰 diff에서 피할 것:

- `작업 내용 정리`처럼 범위가 불명확한 큰 커밋
- 설정 변경과 기능 구현을 한 커밋에 섞기
- 신규 기능과 테스트를 맞추기 위한 별도 버그 수정을 무조건 묶기
- 사용자가 요청한 서로 다른 요구사항을 하나의 `feat`에 모두 넣기
- 전체 변경을 `git add .`로 stage하고 검토 없이 커밋하기

## 커밋 분리 예시

```text
요구사항: User PK는 UUIDv7
커밋: feat : #1 시간순 UUID 생성기 추가

요구사항: MySQL yml 설정과 SQL 경로 관리
커밋: chore : #1 MySQL 및 SQL 관리 설정 추가

요구사항: UserJpaEntity와 domain 분리
커밋: feat : #1 유저 도메인과 JPA 엔티티 분리

요구사항: end_points 권한 테이블
커밋: feat : #1 엔드포인트 권한 테이블 추가

요구사항: JWT/Redis 인증
커밋: feat : #1 JWT Redis 인증 구조 추가
```

## Stage 절차

커밋할 파일을 명시적으로 stage한 후 다음 명령으로 확인한다.

```bash
git diff --cached --stat
git diff --cached --name-only
git diff --cached
```

- staged diff에 사용자의 기존 변경, 비밀정보, 생성물과 다른 의도의 파일이 없는지 확인한다.
- 같은 파일을 hunk 단위로 나눠야 한다면 비대화형 또는 검토 가능한 방법을 우선한다.
- staged diff 전체가 커밋 메시지 한 줄로 설명되지 않으면 다시 분리한다.
- 비어 있는 커밋과 검증되지 않은 자동 생성 파일 커밋을 만들지 않는다.

## 현재 권한·User 작업 분리 후보

```text
chore : #1 AI 작업 규칙 문서 추가
chore : #1 MySQL 및 SQL 관리 설정 추가
feat : #1 유저 도메인 생성
feat : #1 시간순 UUID 생성기 추가
feat : #1 엔드포인트 권한 도메인 생성
feat : #1 JWT Redis 인증 구조 추가
feat : #1 인증 API 추가
```

실제 diff가 크면 security filter, token provider와 auth API를 각각 별도 `feat` 커밋으로 나눈다.
후보는 지시가 아니라 출발점이며 실제 의존성과 검증 가능성을 기준으로 조정한다.

## 커밋 전 확인

가능하면 다음 명령을 실행한다.

```bash
./gradlew test
```

변경 범위에 필요한 통합 테스트, 정적 분석과 `./gradlew build`도 실행한다. 필수 테스트가 실패하면
커밋하지 않는다. 환경 문제로 테스트하지 못했다면 커밋 여부를 작업 지침에 따라 판단하고 PR 또는
작업 요약에 명령, 원인과 잔여 위험을 기록한다.
