# Release and PR Checklist

## 목적

출시, PR 생성, 커밋 전 마지막으로 확인할 항목을 한 곳에 모은다. 이 체크리스트는 실제 작업이
완료되었다는 증거를 남기기 위한 문서이며, 테스트 실패를 숨기기 위한 형식적 절차가 아니다.

## 공통 체크

- [ ] 요청한 작업 범위가 충족되었다.
- [ ] 관련 없는 파일 변경이 포함되지 않았다.
- [ ] `git status -sb`로 작업 트리를 확인했다.
- [ ] `git diff` 또는 `git show`로 변경 내용을 자체 리뷰했다.
- [ ] 비밀정보, 개인정보, 운영 접속정보가 포함되지 않았다.
- [ ] 디버그 로그와 임시 주석이 제거되었다.
- [ ] 새 의존성 추가 시 이유, 대안, 영향 범위를 기록했다.

## Backend 체크

- [ ] `./gradlew test`를 실행했다.
- [ ] 필요한 경우 `./gradlew build`를 실행했다.
- [ ] Controller는 DTO 변환과 UseCase 호출만 담당한다.
- [ ] Entity를 API 응답으로 직접 반환하지 않는다.
- [ ] 인증, 인가, 리소스 소유권 검증이 포함되었다.
- [ ] MySQL 스키마 변경은 migration 전략과 하위 호환성을 확인했다.
- [ ] Redis 키, TTL, 직렬화, 무효화 정책을 확인했다.
- [ ] 주문, 결제, 재고 변경은 멱등성과 동시성을 확인했다.

## Frontend 체크

- [ ] API base URL이 환경변수로 분리되어 있다.
- [ ] BE DTO와 FE 화면 모델 사이에 adapter가 있다.
- [ ] 로딩, 에러, 빈 상태가 화면에서 깨지지 않는다.
- [ ] JWT가 필요한 API에 `Authorization: Bearer <accessToken>`이 전달된다.
- [ ] 민감정보를 localStorage 등에 저장하는 경우 위험을 기록했다.
- [ ] 빌드 또는 타입 체크를 실행했다.

## API 계약 체크

- [ ] 현재 API와 목표 API를 혼동하지 않았다.
- [ ] `/api/v1` 전환은 별도 작업으로 분리했다.
- [ ] `ApiResponse.payload` 전환은 FE 연동과 분리했다.
- [ ] Request DTO와 Response DTO가 HTTP 모양만 표현한다.
- [ ] 날짜와 시간은 ISO-8601 형식으로 표현한다.
- [ ] Swagger/OpenAPI 문서가 실제 API와 일치한다.

## 문서 체크

- [ ] 변경된 API는 `api.md` 또는 Swagger 문서에 반영했다.
- [ ] FE-BE 계약 변경은 `FE_INTEGRATION.md`에 반영했다.
- [ ] 인프라나 DB 권한 변경은 `INFRA_CI.md` 또는 `DATA_REDIS_S3.md`에 반영했다.
- [ ] 도메인 완료 시 Notion 또는 `docs/domains/<domain>.md`를 갱신했다.
- [ ] 포트폴리오에 기록할 만한 설계 결정과 검증 결과를 남겼다.

## PR 본문 체크

- [ ] 작업 내용이 실제 diff 기준으로 작성되었다.
- [ ] 검증 명령과 결과가 적혀 있다.
- [ ] DB, Redis, S3, API 계약, 배포 영향이 명시되었다.
- [ ] 다음 작업과 잔여 위험이 적혀 있다.
- [ ] 관련 이슈가 연결되어 있다.

## 완료 불가 조건

아래 조건 중 하나라도 있으면 완료로 보고하지 않는다.

- 테스트 실패를 알고도 커밋한다.
- 민감정보가 포함되어 있다.
- 사용자 변경을 함께 커밋한다.
- 운영 DB, 운영 자격증명, 배포 설정을 승인 없이 변경한다.
- 현재 구현과 향후 목표를 섞어 실제 동작처럼 설명한다.
