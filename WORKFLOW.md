# Workflow, Documentation and Portfolio Guide

## 자동 커밋

- 커밋하기 전에 `commit.md`의 메시지 형식, prefix와 diff 분리 규칙을 적용한다.
- 변경 범위의 단위 테스트 후 통합 테스트, 정적 분석과 빌드를 실행한다.
- 필수 검증 실패 또는 미해결 오류가 있으면 커밋하지 않는다.
- `git diff`를 자체 리뷰한 후 하나의 논리적 작업 단위로 커밋한다.
- 이번 작업 파일만 stage하고 사용자의 기존 변경을 포함하지 않는다.
- 저장소 규칙이 없으면 명령형의 간결한 커밋 메시지를 사용한다.
- amend, rebase, force push와 기존 커밋 수정은 명시적 요청 없이 수행하지 않는다.
- 자동 커밋은 수행하되 원격 push와 PR 생성은 요청 범위에 있을 때만 수행한다.
- 통합 검증을 환경 문제로 못 했다면 잔여 위험을 최종 보고한다.

## 도메인 문서화

도메인은 요구 API, 상태 전이, 테스트, API 문서, MySQL·Redis·S3 검증과 커밋이 완료됐을 때
문서화 단계로 이동한다.

- Notion 연결이 있으면 Ecommerce 데이터베이스의 기존 구조와 문서를 확인해 생성 또는 갱신한다.
- 같은 도메인 페이지를 중복 생성하지 않는다.
- 연결이 없으면 `docs/domains/<domain>.md`에 동등한 초안을 작성하고 미동기화 이유를 보고한다.
- Notion에는 목적·사용자 가치, 기능/API, 설계 결정, 데이터·인프라, 테스트 결과,
  트러블슈팅과 다음 작업을 기록한다.
- 비밀정보, 개인정보와 운영 접속정보를 문서에 넣지 않는다.

## 포트폴리오

- 기존 포트폴리오가 없으면 `docs/portfolio.md`를 생성한다.
- 도메인별로 문제, 직접 구현한 범위, 핵심 기술, 대안과 트레이드오프, 문제 해결,
  검증 결과와 다음 개선을 기록한다.
- 측정하지 않은 성능, 사용자 수와 개선율을 만들지 않는다.
- 수치를 사용하면 측정 환경·방법과 근거를 함께 기록한다.
- 문서 변경은 코드 커밋과 분리한 문서 커밋으로 만들 수 있다.

## PR 본문

```md
## 작업 내용
- 구현 또는 변경한 동작
- 주요 설계 결정
- DB, Redis, S3와 API 계약 영향

## 검증
- 실행 명령과 결과
- API 문서, Docker와 DB 권한 확인 결과

## 다음 작업
- 이번 범위에서 제외한 후속 작업
- 기술 부채, 운영 보완과 남은 위험

## 문서화
- Notion 페이지
- 포트폴리오 문서
```

- PR 제목과 설명은 실제 diff 기준으로 작성한다.
- 호환성을 깨는 변경과 배포 순서를 명확히 표시한다.
- 다음 작업이 없으면 `없음`으로 기록한다.
- 관련 이슈를 연결하고 최신 커밋의 검증 결과를 다시 확인한다.

## 완료 정의와 최종 보고

다음을 모두 충족해야 완료다.

- 요구 동작, 예외 경로와 권한 검사가 구현되었다.
- 필요한 테스트와 빌드가 통과했다.
- API·DB·Redis·S3·보안·배포 영향이 검토되었다.
- API 문서와 관련 기술 문서가 실제 동작과 일치한다.
- 도메인 완료 시 Notion과 포트폴리오가 갱신되었다.
- 검증된 논리적 작업 단위 커밋이 생성되었다.

최종 보고에는 변경 이유, 검증 명령과 결과, 호환성 영향, 커밋 해시·메시지, 문서 링크,
미실행 검증과 잔여 위험을 포함한다. 테스트 실패 상태를 완료로 표현하지 않는다.
