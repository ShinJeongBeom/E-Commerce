# Testing and Verification Guide

## 테스트 전략

- 도메인과 Service 규칙: 빠르고 결정적인 단위 테스트
- Controller 계약·검증·상태 코드·인가: MockMvc 또는 기존 API 테스트
- JPA·락: 실제 MySQL 기반 통합 테스트
- QueryDSL과 마이그레이션 도구를 도입한 뒤에는 해당 검증을 추가한다.
- Redis TTL·직렬화·무효화·락: 실제 Redis 기반 통합 테스트
- 전체 핵심 흐름: 필요할 때 Spring Boot 통합 또는 E2E 테스트
- 현재는 프로젝트 기존 테스트와 로컬 Docker Compose 기반 MySQL·Redis 확인을 우선한다.
- Testcontainers는 통합 테스트 격리가 필요해질 때 별도 작업으로 도입한다.
- H2만으로 MySQL 고유 SQL, 인덱스, 락과 타입 동작을 증명하지 않는다.

## 작성 원칙

- 버그는 수정 전 실패하고 수정 후 통과하는 회귀 테스트를 먼저 작성한다.
- 테스트 이름은 조건, 행동과 기대 결과를 드러낸다.
- 시간·UUID·난수는 `Clock` 또는 기존 주입 방식을 사용해 결정적으로 만든다.
- 내부 구현을 과도하게 mocking하지 말고 외부 경계만 대체한다.
- 성공 경로뿐 아니라 검증 실패, 미인증, 권한 부족, 상태 충돌과 외부 장애를 확인한다.
- 테스트 간 데이터·Redis 키·실행 순서 의존성을 없앤다.
- 커버리지 수치만 목표로 삼지 않고 위험한 분기와 도메인 불변식을 우선한다.

## 변경 범위별 검증

| 변경 | 필수 검증 |
|---|---|
| 순수 로직 | 관련 단위 테스트, 전체 단위 테스트 |
| Controller·DTO | Service 테스트, API 계약·인가 테스트 |
| Entity·Repository | MySQL 통합 테스트 |
| QueryDSL·마이그레이션 도입 후 | 조건 조합, migration 검증 |
| Redis | Redis 통합, 캐시 미스·장애·TTL 경로 |
| 주문·결제·재고 | 동시성, 멱등성, 롤백과 상태 전이 |
| 보안·공통 모듈 | 전체 테스트와 빌드 |
| 빌드·의존성 | clean build, 컨텍스트 시작과 보안 검사 |
| 문서 | 링크, 경로, 명령과 실제 계약 일치 여부 |

## 실행

Gradle Wrapper와 저장소 설정을 사용한다.

```bash
./gradlew test
./gradlew build
```

멀티 모듈은 변경 모듈 테스트 후 전체 테스트를 실행한다. Docker나 네트워크 권한 때문에 검증하지
못하면 우회해 성공으로 간주하지 말고 명령, 원인과 잔여 위험을 보고한다.

## 실패 처리

1. 첫 실패와 최초 원인을 분리한다.
2. 제품 코드, 테스트 fixture, 환경 설정 중 원인을 판별한다.
3. 테스트 삭제, 무시 또는 assertion 약화 없이 수정한다.
4. 같은 좁은 검증을 재실행한다.
5. 통과하면 영향 범위 검증으로 확장한다.
6. 반복 실패 시 명령, 핵심 로그, 시도와 가설을 기록하고 접근을 변경한다.

## 완료 증거

최종 보고에 실행 명령과 통과·실패·미실행 결과를 정확히 적는다. 단위 테스트만 통과하고 통합
환경을 확인하지 못했다면 완전 검증으로 표현하지 않는다.
