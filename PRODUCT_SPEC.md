# Ecommerce Product Specification

## 역할 모델

현재 코드의 역할 enum은 `Role.USER`, `Role.ADMIN`이다. 요구사항의 `CUSTOMER`는 `Role.USER`의
업무 명칭이며 별도 역할로 중복 정의하지 않는다. `Role.SELLER`는 판매자 도메인 작업에서 추가한다.

| 역할 | 업무 명칭 | 설명 |
|---|---|---|
| `USER` | CUSTOMER | 일반 회원 및 구매자 |
| `SELLER` | SELLER | 관리자 승인을 받은 판매자. 현재 미구현 |
| `ADMIN` | ADMIN | 사이트 전체 운영자 |

- 가입 직후 역할은 `USER`다.
- 판매자 신청 정보는 향후 `SellerProfile`에서 관리한다.
- 판매자 상태는 향후 `PENDING`, `APPROVED`, `REJECTED`, `SUSPENDED`, `WITHDRAWN`을 지원한다.
- 승인 시 `APPROVED` 전환과 `SELLER` 역할 부여를 한 트랜잭션으로 처리한다.
- 정지·탈퇴 시 판매 기능을 차단하고 진행 중 주문과 정산 처리 정책을 적용한다.
- 관리자 역할은 공개 API로 생성하거나 변경하지 않는다.

## 역할별 기능

### USER(CUSTOMER)

- 회원가입, 로그인, 토큰 재발급과 로그아웃
- 상품 목록·상세 조회와 검색
- 장바구니 추가·수량 변경·삭제
- 주문 생성, 결제와 배송 조회
- 본인 주문 내역 조회
- 배송 완료된 구매 상품의 리뷰 작성·수정·삭제
- 찜 추가·해제·목록 조회

### SELLER

현재 미구현이며 다음 기능을 목표로 한다.

- 판매자 프로필 조회
- 본인 상품 등록·수정·판매 중지·삭제와 재고 관리
- 본인 상품이 포함된 주문 항목 조회
- 배송 준비·시작·완료 처리
- 본인 매출 통계와 정산 내역 조회

### ADMIN

현재 기본 역할만 존재하며 다음 기능을 목표로 한다.

- 회원 조회·정지·탈퇴 처리
- 판매자 신청 조회·승인·거절·정지·탈퇴 처리
- 전체 상품 조회와 부적절 상품 판매 중지·삭제
- 전체 주문 조회와 환불 처리
- 공지사항·이벤트·배너 관리
- 중요한 관리 작업의 사유와 감사 로그 기록

## 핵심 사용자 흐름

```text
구매자: 회원가입 -> 로그인 -> 상품 탐색 -> 상세 -> 장바구니
       -> 주문 -> 결제 -> 배송 조회 -> 배송 완료 -> 리뷰

판매자: 회원가입 -> 판매자 신청 -> 관리자 승인 -> 상품 등록
       -> 주문 확인 -> 배송 준비 -> 배송 시작 -> 배송 완료

관리자: 로그인 -> 판매자 신청 처리 -> 상품 관리 -> 회원 관리
       -> 전체 주문/환불 -> 사이트 콘텐츠 관리
```

선행 조건이 충족되지 않으면 API는 명시적인 도메인 오류를 반환한다.

## 상태 전이

- 상품: `DRAFT -> ON_SALE -> SOLD_OUT | SUSPENDED | DELETED`
- 주문: `PENDING_PAYMENT -> PAID -> PREPARING -> SHIPPED -> DELIVERED`
- 취소·환불: `CANCELED` 또는 `REFUND_REQUESTED -> REFUNDED`
- 결제: `READY -> APPROVED | FAILED | CANCELED | REFUNDED`

현재 코드의 주문 상태명은 다음 전이만 허용한다.

```text
CREATED -> PAID -> PREPARING -> SHIPPED -> DELIVERED
    |
    +-----> CANCELLED
```

- `CANCELLED`, `DELIVERED`는 종료 상태다.
- `PAID` 이후 취소는 단순 주문 취소가 아니라 결제 취소·환불 흐름에서 처리한다.
- 주문 취소 트랜잭션은 주문을 잠근 뒤 상태를 확인하고 재고를 한 번만 복구한다.
- 주문 상품은 상품 참조와 별개로 주문 당시 상품명, 단가와 수량을 스냅샷으로 저장한다.
- 주문 총액은 주문 당시 단가와 수량으로 계산하며 이후 상품 정보 변경의 영향을 받지 않는다.
- 주문번호는 결제 연동 식별자로 사용되므로 UUID 기반으로 생성하고 DB 유니크 제약을 적용한다.

Service에서 허용된 전이만 수행한다. 결제 콜백은 멱등 처리하고 결제 승인, 주문 확정과 재고
차감의 정합성을 보장한다. 재고는 음수가 될 수 없다. 여러 판매자의 상품을 한 주문에 담을 수
있으므로 배송과 정산은 판매자별 주문 항목 단위 상태를 지원한다.

## 도메인 완료 조건

- 인증·회원: 가입, 로그인, 재발급, 로그아웃, 정지 계정과 역할 검증
- 판매자: 신청·승인·거절·정지 전이와 권한 상승 방지
- 상품: 소유권, 검색, 재고, 이미지 보상 처리
- 장바구니·찜: 사용자 격리, 중복 방지, 수량 제한
- 주문·결제: 가격 스냅샷, 재고 동시성, 멱등성, 실패 롤백
- 배송·리뷰: 상태 전이, 구매·배송 완료 확인, 중복 리뷰 방지
- 관리자: 인가 차단, 처리 사유와 감사 로그
